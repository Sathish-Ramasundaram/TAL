Google Search Analogy

Typing a query and hitting Search

The latest search result is shown on screen → useState

The query itself is stored in memory (history) → useRef

Clicking History

Pull all previous queries from useRef

Update the UI with useState to display them

