function ARIA() {
    return(
        <>

        {/* Bad (Use only if no other option) */}
        
        <button aria-label="Close dialog">
            X
        </button>
        </>
    );
}

export default ARIA;